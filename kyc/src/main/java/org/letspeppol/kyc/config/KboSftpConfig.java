package org.letspeppol.kyc.config;

// Deprecated SFTP config no longer used after switching to plain JSch client.
// Intentionally left empty to avoid compilation issues; can be removed later if not needed.

