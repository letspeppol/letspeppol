GRAFANA_DOMAIN=
CERT_RESOLVER=
DISCORD_CHANNEL=
DISCORD_WEBHOOK_URL=
SERVER_NAME=
MINIO_ROOT_USER=mimir
# If you change MINIO_ROOT_PASSWORD from the default, also update the hardcoded credentials in
# docker/monitoring/mimir/config.yml and docker/monitoring/loki/config.yml (they read the same secret).
MINIO_ROOT_PASSWORD=
GRAFANA_USER=admin
GRAFANA_PASS=