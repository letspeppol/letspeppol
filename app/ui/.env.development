# Local development API endpoints
# In dev, Vite proxies /kyc, /app, /proxy to the backend services.
# No need to set VITE_*_BASE_URL — the code defaults to /kyc, /app, /proxy.
VITE_PEPPOL_DIR_URL=https://test-directory.peppol.eu
